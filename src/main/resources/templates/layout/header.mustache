<!DOCTYPE HTML>
<html>
<head>
    <!--
    페이지 로딩속도를 높이기 위해 css는 header에 js는 footer에 둔다.
    HTML은 위에서부터 코드가 실행되기 때문에 head가 실행되고 body가 실행됌
    css는 화면을 그리는 역할로 head에 불러오는 것이 좋다.
    bootstrap.js의 경우 꼭 제이쿼리가 있어야만 하기 때문에 부트스트랩보다 먼저 호출되로록 코드를 작성하는 것이 좋다.
    보통 bootstrap.js가 제이쿼리에 의존한다고도 말한다
    -->
    <title>스프링부트 웹서비스</title>
    <meta http-equiv="Content-Type" content="text/html"; charset="UTF-8" />

    <link rel="stylesheet" href="http://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
</head>
<body>
